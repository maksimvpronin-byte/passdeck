# Известные проблемы

На момент финализации PassDeck 0.2.0 блокирующих известных проблем не зафиксировано.

## Исправлено в 0.2.0

- [x] Auto-Type больше не восстанавливает, не активирует и не изменяет размер целевого окна перед вводом.
- [x] При смене активного окна Auto-Type отменяется до передачи логина или пароля.
